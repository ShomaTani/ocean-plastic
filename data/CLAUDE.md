# TRACE — Trajectory Reversal for Attribution of Coastal Emission

海洋プラスチック漂着物の起源推定。深層学習による順方向・逆方向モデルの構築。
情報α 最終課題。

---

## 1. プロジェクトの目的

海洋プラスチックごみの漂流をシミュレーションし、2方向のモデルを深層学習で構築する。

| 方向 | 入力 | 出力 | 位置づけ |
|---|---|---|---|
| **順モデル** | 放出源 | 漂着分布ヒートマップ | 逆モデルの微分可能サロゲート + 高速推論 |
| **逆モデル** | 観測された漂着地点 | 起源の確率分布(「責任マップ」) | **本プロジェクトの新規性の核** |

最終的な狙いは、海洋プラスチック汚染の社会的責任を定量的・可視的に示すこと。

### 順モデルにNNを使う理由(重要)

順モデルの精度上限はシミュレータそのものであり、**NNがシミュレータより正確になることは原理的にない**。
それでもNNを使う理由は精度ではなく以下の3点。発表でもこの立場で説明する。

1. **速度(償却推論)** — シミュレータは1ケース数分〜数十分。NNならミリ秒。Streamlitでの
   インタラクティブ可視化(地図をクリック→即座にヒートマップ)はNNでしか成立しない。
2. **微分可能性** — `parcels` は内部でCコンパイルされた数値積分であり微分不可能。
   逆モデルの訓練ループに物理整合性ペナルティを組み込むには、微分可能な順方向
   サロゲートが必要。ここがNNの本質的な役割。
3. **精度の参照基準** — 順モデルがシミュレータをどれだけ模倣できるかが、
   逆モデル評価の上限の目安になる。

「順モデルもNNの方が高精度」とは**主張しない**(根拠のない誇張になる)。

---

## 2. 現在の進捗

### 完了済み

- **データ取得**
  - GLORYS12V1 表層流速 (`uo`, `vo`) 2018–2022年、東経120–150 / 北緯25–50、日次、757MB
    - Product ID: `GLOBAL_MULTIYEAR_PHY_001_030`
    - Dataset ID: `cmems_mod_glo_phy_my_0.083deg_P1D-m`
    - 解像度 0.083度(約9km)、グリッド 361 × 301
  - ERA5 10m風速 (`u10`, `v10`) 同領域・同期間、6時間毎 (CDS経由)
- **シミュレータ構築** (`trace_sim_test.py`)
  - 素の `parcels` (v3.1.4) + 自作カーネル。`plasticparcels` は不使用(理由は §5)
  - RK4移流 + windage + land-mask beaching + 境界離脱処理
- **本番シミュレーション実行済み(2回目、データ増量後)**
  - **500地点 × 200粒子 = 100,000粒子、90日間追跡**(当初300地点×50粒子から増量)
  - 結果: 漂着80% / 境界離脱15% / 平均移動距離2.46度 / 最大24.1度
  - 実行時間は当初想定(300地点×90日で一晩コース)よりずっと短く、100,000粒子でも
    約40秒で完走した(§9の想定は過剰見積もりだった)
- **越境輸送の検証**(300地点版のデータで実施。数値は当時のまま)
  - 韓国沿岸発の44.2%が日本に到達。日本海横断は主要な輸送経路として機能している
  - 日本漂着粒子の起源内訳: 日本92.9% / 韓国6.6% / 中国0.3%
- **可視化・診断スクリプト群** (§4参照)
- **学習データの整形** (`inputdata_dim.py`, `build_current_field.py`, `make_split.py`,
  `build_backward_pairs.py`, `make_split_backward.py`)
  - 放出地点をガウシアンで滲ませた入力マップ + 海流場(平均u, v)を追加チャンネル化
  - 地理クラスタ単位のgrouped split(500地点版: train 408 / val 64 / test 28)
- **順モデル(Attention U-Net)の実装・訓練** (`forward/`) — 動く状態まで到達
  - 自前実装(`main.py`: ConvBlock/DownSample/AttentionGate/UpSample/AttentionUNet)
  - 10回以上の実験(`forward/experiments.txt`に全ログ)を経て、現状のベスト構成:
    3ch入力(gaussian, u, v) / LR=3e-3 / weight_decay=1e-3 / cross_entropy loss /
    masked softmax(沿岸限定、dilation=3) / dropout・スケジューラ無し
  - 500地点版で再訓練済み。詳細は§6論点A参照
- **逆モデル(Attention U-Net)の実装・訓練** (`backward/`) — 動く状態まで到達
  - forward/main.py, forward/losses.pyをimportlib経由で再利用(ファイル名衝突を
    importlibのspec_from_file_locationで回避)
  - 7回以上の実験(`backward/experiments.txt`)を経て、現状のベスト構成:
    LR=3e-3 / weight_decay=1e-5(forwardと違う値が最適) / cross_entropy /
    masked softmax(沿岸限定、dilation=1) / 出力にもガウシアンぼかし / n_origins層化split
  - 500地点版で再訓練済み。best val=2.95
- **Streamlit実装** (`streamlit_app.py`) — 動く状態まで到達
  - 順/逆モデルをガラス風セグメントコントロールで切り替え、地図クリック→
    最寄り沿岸に自動補正→推論→責任マップ/漂着分布を表示
  - `streamlit-image-coordinates`でクリック座標取得。`streamlit==1.38.0`に
    固定(最新版だと非互換)。詳細は§5参照

### 次にやること

1. 環境省実測データとの整合性チェック
2. レポート執筆
3. 余裕があれば: 論点B(空間分解能)、windage較正(論点D)

余裕があれば: 推論時間・精度のシミュレータ比較(NN利用の意義を定量的に示せる、費用対効果が高い)

**やらない(今後の拡張として明記)**: 沈降(settling)、生物付着(biofouling)。
沈降は水温・塩分・複数深度層のデータ再取得とシミュレーション再実行が必要で、
残り時間に対して重すぎる。「表層浮遊のみを対象」とスコープを明示する。

---

## 3. データ構造

### 学習に使うファイル(シミュレーション出力)

| ファイル | 形状 | 内容 |
|---|---|---|
| `density_maps_all.npy` | `(500, 128, 128)` | origin別、全粒子(浮遊中含む)の最終位置密度 |
| `density_maps_beach.npy` | `(500, 128, 128)` | origin別、**漂着済みのみ**の密度。基本こちらを使う |
| `release_sites_used.csv` | 500行 | `site_id, lon, lat`。origin indexとの対応表 |
| `trace_run.zarr` | `(100000, ~90)` | 全軌跡。**学習には使わない**。Streamlitの漂流パス可視化用 |

(データ増量前は300地点×50粒子だった。詳細は§6論点A参照)

- 各密度マップは合計1に正規化済み(確率分布)
- 境界離脱した粒子は密度マップから除外済み
- グリッド: 128×128 で東経120–150 / 北緯25–50 をカバー → **1セル約0.23度(26km)**

### 学習に使うファイル(NN入力に整形済み、`inputdata_dim.py`等の出力)

| ファイル | 内容 |
|---|---|
| `train_pairs.npz` | `X`(放出点ガウシアン, 500,128,128), `Y`(=density_maps_beach), `split`(train/val/test), `valid`(漂着ゼロフラグ), `site_id`, `lon`, `lat` |
| `current_field.npz` | 全site共通の海流場。`u`, `v`(平均、標準化済み、陸=0), `speed`(不採用, §5参照), `land_mask` |
| `backward_pairs.npz` | 逆モデル用。`X_gaussian`(観測点ガウシアン), `Y`(責任マップ、ガウシアンぼかし+沿岸マスク済み), `split`, `n_origins`, `row`, `col`, `lon`, `lat` |

`forward/dataset.py`の`TraceDataset`、`backward/dataset.py`の`BackwardDataset`が
それぞれ読み込んで、ガウシアン+u+vの3ch入力を組み立てる。

### `.zarr` の中身(軌跡データ)

次元: `trajectory`(15000) × `obs`(約90、24時間毎)

変数: `lon`, `lat`, `z`, `time`, `beached`, `left_domain`, `prev_lon`, `prev_lat`,
`origin`(once), `windage`(once)

現状の後処理は `obs=0`(出発)と `obs=-1`(最終)しか使っていない。
中間軌跡はStreamlitのパス可視化で初めて使う。

---

## 4. ファイル一覧

### パイプライン本体
| ファイル | 役割 |
|---|---|
| `era5_download.py` | ERA5風データ取得。`download_format: unarchived` でzip回避 |
| `release_site.py` | land maskから沿岸セルを抽出し300地点を自動生成 |
| `sim_main.py` | **シミュレータ本体**。CONFIGブロックで全パラメータ管理 |

### 診断・可視化
| ファイル | 役割 |
|---|---|
| `raw/check_glorys.py` | GLORYSファイルの中身検証(変数名・範囲・NaN率・値域) |
| `plot_currents.py` | 海流ヒートマップ + ベクトル場 |
| `inspect_density.py` | 密度マップの数値診断(非ゼロセル数・集中度) |
| `plot_all.py` | 全origin集約 + 陸地マスク重ね |
| `check_cross_transport.py` | 越境輸送の輸送行列(origin地域→漂着地域) |
| `plot_cross_transport.py` | 越境軌跡のみを線で描画 |
| `plot_distance_histogram.py` | 移動距離の棒グラフ(同地域/越境で色分け) |

未実装(将来の拡張候補。分解能が粗すぎると判断した場合にレポート改善案として実装しうる):
放流点ズーム表示、対数スケール表示、NetCDF断面のCSV化

---

## 5. 重要な設計判断と、踏んだ地雷

過去に実際に踏んで修正したバグ。同じ轍を踏まないこと。

### 単位変換の罠(2回踏んだ)
- `fieldset.UV[...]` は spherical mesh で **m/s → 度/s に自動変換される**
- 自分で `add_field` した フィールドは `mesh='flat'` にすると**生のm/sのまま返る**
- 初期のbeaching判定で「速度 < 1e-3 m/s なら漂着」としたら、実際には度/s空間で
  比較していて**全粒子が即座に漂着**した。→ land-maskベースの判定に変更して解決
- windageカーネルでは `deg_per_m_lat = 1/111319.5`、`deg_per_m_lon = 1/(111319.5*cos(lat))`
  で**手動換算**している。検証済み(10m/s × 2% × 4日 = 理論値0.62度 ≒ 実測0.6179度)

### 漂着と境界離脱の混同
- 当初 `ErrorOutOfBounds` を `beached=1` にしていたため、**東経150度(領域の端)に
  沿って偽の"漂着"が並んだ**
- → `beached`(陸マスク接触) と `left_domain`(領域外離脱) を別フラグに分離。
  密度マップは領域内粒子のみで集計

### ジッタが陸に落ちる
- 放流点は「陸に隣接する海セル」なので海岸線のすぐ隣。`JITTER_DEG=0.05`(±0.05度)は
  グリッド解像度0.083度とほぼ同じ → ジッタで隣の陸セルに飛ぶ
- 地点中心しか陸判定していなかったため、**出発した瞬間に漂着扱いになる粒子**が発生
  (検証では条件次第で22.6%が陸ヒット)
- → 1粒子ずつジッタ後の座標を陸判定し、陸なら再サンプリング

### ERA5 (新CDS) の癖
- 時間座標が `time` ではなく **`valid_time`** のことがある
- 緯度が**降順(N→S)**。`sortby` で昇順化が必要
- 風フィールドの `time` 引数には **`datetime64`配列をそのまま渡す**。
  秒数に変換して渡すと `time origins of different grids must be have the same type` で落ちる
- 複数変数を同時リクエストすると**zipで返る**。`download_format: "unarchived"` で回避

### plasticparcels を使わない理由
- 公式の例はバンドルされた `NEMO0083` データと全球の放流マップ前提
- 自前のGLORYSと自己定義の300地点を食わせるには独自settings JSONの作成が必要で、
  それ自体が大仕事
- 必要な機能(windage, beaching)は素の `parcels` に数十行で書ける
- → 素の `parcels` + 自作カーネルが正解

### その他
- **絶対パスは使わない**。`Path(__file__).resolve().parent` 基準の相対パスにする
- GLORYS等の重いデータは `.gitignore` 済み。認証情報(`.cdsapirc` 等)も除外
- 可視化は必ずズーム/対数スケールで確認する。全域30度四方の線形スケールだと
  近傍集中に埋もれて何も見えない(何度も「真っ暗な図」で混乱した)
- torch実行時に `OMP: Error #15`(OpenMP重複)が出ることがある。
  `KMP_DUPLICATE_LIB_OK=TRUE` を環境変数に付けて回避(根本原因は未調査)

### 順モデル訓練で踏んだ地雷(forward/、実験ログは`forward/experiments.txt`)

- **weighted_mse損失は「定数出力」に崩壊する罠**: 海流場(u, v)を入力チャンネルに
  追加した際、全site共通で変動の少ない支配的な信号があると、weighted_mse損失では
  モデルが「入力を無視して常に同じ1枚を返す」局所解に落ちた(バッチ内予測分散が
  1e-7〜1e-8まで潰れる)。LR調整では解決せず、**cross_entropy損失(softmaxで
  sum=1にしてから比較)に切り替えて解決**。スパースな確率分布ターゲットに対しては
  cross_entropyの方が頑健
- **入力表現だけでは不十分、loss設計も同じくらい重要**: 論点Aの「座標のみ入力」
  (ガウシアンのみ)では、モデルは入力をコピー/ぼかすだけで海流輸送を学習しなかった
  (予測ピークと入力ピークの距離が平均0.1px)。海流チャンネルを足しても
  weighted_mseのままでは上記の崩壊が起きたため、**入力表現の改善とloss設計の
  改善は両方必要だった**
- **チャンネルを増やすほど良いわけではない**: 流速の大きさ`speed=√(u²+v²)`を
  4ch目として追加したが悪化(val 2.965→3.964)。u, vから決定的に計算できる
  冗長な情報を足しても、この小データ規模では過学習を悪化させるだけ
  だった。効果はデータ規模との兼ね合いで決まる
- **正則化はweight_decayが効いた、Dropoutは重ねると逆効果**: Adamの
  `weight_decay=1e-3`でtrain-valのgapが1.78→0.50に改善。同じ状態に
  Dropout2dを追加(0.1〜0.3)すると全て悪化(trainにすら十分フィットできなく
  なった)。**正則化手法は1つずつ切り分けて試すこと**、闇雲に重ねない
- **乱数シード固定は必須**: `torch.manual_seed`を訓練の頭で固定しないと、
  実験間で「設定の効果」と「初期値運」が区別できない。固定してからは、
  同じ設定なら別プロセス・別実行でも結果がビット単位で再現された
  (デバッグ時に「本当に重みが変わったか」の確認にも使えた)
- **300地点という空間サンプリングの粗さが精度上限になっている可能性が高い**:
  test(未知クラスタ)では「その地域一帯の大まかな輸送方向」は捉えられるが、
  クラスタ内の地点ごとの細かい違いまでは再現できない。loss設計・正則化は
  一通り試した後の結論として、データ量(追加シミュレーション)が次の
  最有力レバーと判断 → **実際に500地点へ増量したところ、これが正しかったと
  裏付けられた**(詳細は§6論点A)

### masked softmaxの発見と、順・逆モデルでの扱いの違い

- Streamlitで任意の新規座標(学習データに無い点)を推論すると、**確率質量の
  大部分が物理的にありえない場所(海のど真ん中/内陸)に漏れている**ことが
  発覚。cross_entropyは「正解がある場所の確率を上げる」ことしか罰則にしないので、
  「関係ない場所にどれだけ確率を撒いても罰則ゼロ」という抜け穴があった
- 対策: softmax前に非許可セルのロジットを`-inf`にするmasked softmax
  (`forward/losses.py`の`to_prob_map(logits, mask=...)`)。**mask使用時は
  target側も同じmaskで切り落として再正規化しないと、モデルが原理的に
  出力不可能な場所にtargetの質量が残ったままになり、lossが爆発する**
  (val loss 4.57→13.5に悪化する事故を実際に踏んだ)
- **順モデルと逆モデルで「許容範囲」の定義が違う**点に注意:
  - 逆モデル(origin): 常に沿岸(陸に接した海セル)のはず → dilation=1で厳密に絞ってよい
  - 順モデル(beaching destination): 粗い128グリッドの陸マスクは小島や複雑な
    海岸線を取りこぼすため、dilation=1だと正解Yの48%が「非沿岸」判定される
    誤検出になる。dilation=3(緩め)を採用
- masked softmax単体では「未知点での過剰な広がり(ハルシネーション)」の
  根本解決にはならなかった。海のど真ん中への漏れは防げるが、沿岸沿いに
  実際より遥かに広い範囲へ広がる問題は別原因(データの疎さ)だった(§6論点A)

### Streamlitアプリ実装で踏んだ地雷

- `streamlit-image-coordinates`(クリック座標取得用コンポーネント)が最新の
  streamlit(1.61系)と非互換(`ImportError`)。**`streamlit==1.38.0`に固定**して解決
- `st.container(key=...)`はstreamlit 1.38.0に無い(1.31+の一部バージョンから)。
  構造的なセレクタ(`:first-of-type`等)で代用しようとすると、Streamlitの
  内部DOM構造をたまたま拾って**ページ全体をposition:fixedにしてしまう事故**が
  起きた。→ ページ内に1つしかない要素(`div[data-testid="stRadio"]`等)を
  直接CSSで狙う方式に変更して解決
- Streamlit標準ヘッダー(Deployボタン等)が自作の固定ヘッダーの上に被って
  文字を隠すことがある。`header[data-testid="stHeader"] { display: none; }`で対応
- カスタムコンポーネント(`streamlit_image_coordinates`)は**常にiframeの中に
  描画される**。iframe自体は親の全幅を取り、中の画像はiframe内で左寄せになる
  ため、画像を画面中央に置きたい場合はiframe自体の幅をCSSで画像サイズに
  合わせてから`margin: 0 auto`で中央寄せする必要がある
- PlaywrightでUIを自動検証する際、`position: fixed`要素があると
  `full_page=True`のスクリーンショットが正しく撮れないことがある
  (スクロール撮影が固定要素と干渉する)。通常のビューポート内スクリーンショットで代用

### git運用で踏んだ地雷

- `main`と`tani`(作業ブランチ)を行き来している間に、**GitHub PRのマージが
  コンフリクトマーカー(`<<<<<<<`等)を解消せずにそのままコミットされる事故**が
  起きた(ローカルでのmerge中断とGitHub Web UIでのPRマージが噛み合わず、
  中身が壊れた状態のままマージされた)
- 原因: `push`は「ローカル→リモート」の一方向、`fetch`は「リモート→ローカル」の
  一方向で、**片方をやっても他方は起きない**。GitHub Web UI上での操作
  (PRマージ、コンフリクト解消エディタでの直接コミット)は、`fetch`しない限り
  ローカルの`git`には一切見えない
- 復旧: `git merge --abort`で中断中のmergeを解消 → `git reset --hard origin/main`で
  リモートの状態に強制的に合わせる → 残っていたマーカーや、巻き戻ってしまった
  ファイル(backward側の実験7回分が丸ごと消えていた)を手動で復元
- 教訓: ブランチを跨いだ作業をするなら、merge/PR前に必ず`fetch`、mergeした
  直後に`git log --oneline -5`で「本当に意図通り取り込まれたか」を確認する

---

## 6. 次のステップの設計論点

### 論点A: 順モデルの入力表現 — 解決済み(ハルシネーション問題も対応済み)

2(ガウシアン) + 3(海流チャンネル)の組み合わせで決着。放出点をガウシアンで
滲ませたマップ + 平均u, v(GLORYS時間平均、128×128にダウンサンプリング済み、
`data/current_field.npz`)の3chを入力にしている。

ガウシアンのみ(海流無し)だと入力をコピーするだけで輸送を学習しなかったため、
海流チャンネルの追加は必須だった(詳細は§5「順モデル訓練で踏んだ地雷」)。
speedチャンネル(4ch目)は冗長で悪化したため不採用。

**追加で発覚した問題**: Streamlitで学習データに無い新規座標(例: 四国沿岸)を
推論すると、実際のシミュレーションでは絶対に起きないはずの広範囲(北海道まで)に
確率が広がる**ハルシネーション**が起きていた。四国周辺の実データ(site 74等)は
漂着が北緯33〜34度台に完全に収まるのに、モデルは北緯43度以北にも9.6%の確率質量を
割いていた。

原因の切り分け:
1. masked softmax(沿岸限定)を試したが、**「海のど真ん中への漏れ」は防げても
   この問題自体はほぼ改善しなかった**(43度以北 9.6%→9.6%)
2. `N_PER_SITE`(1地点あたりの粒子数)を50→200に増やしたら多少改善(9.6%→5.7%)。
   ただし粒子数を増やすのは「既存地点の密度マップを滑らかにする」効果であって、
   空間カバレッジの改善にはならない
3. **origin地点数を300→500に増やしたところ、43度以北への漏れが0.74%まで激減**
   (500地点×200粒子)。300地点という空間サンプリングの疎さが、未知点での
   外挿(ハルシネーション)の主因だったことが実証された

結論: **入力表現・loss設計を工夫するより、訓練データの空間密度(origin地点数)を
増やす方が、未知地点への汎化には圧倒的に効く**。実行時間も当初の想定
(300地点で一晩コース)よりずっと短く(500地点×200粒子=10万粒子でも約40秒)、
時間コストの割に効果が大きい改善だった。

季節変動(放流時期を変えた追加シミュレーション)は依然として未着手。
時間があれば拡張候補として残る。

### 論点B: 空間分解能(未確定・要検討)

- 現状の128×128グリッドは1セル約26km。**GLORYSの元解像度(9km)より粗い**
- ズームインしても元データの細かさは戻らない
- 選択肢:
  - **A. 全域高解像度化** (361×301) — NN入出力が6.4倍。MacBook Air M5/16GBでは
    Attention U-Netの訓練が現実的でないリスク
  - **B. 二段階構成** — 全域は128×128、注目領域(日本海側等)だけ別途128×128で切り出せば
    0.08度(9km)相当に到達。**シミュレーションのやり直しは不要**(`.zarr`から
    グリッドを切り直すだけ)だが、NN訓練が2周分
  - **C. 現状維持 + 分解能を仕様として明示** — 最も軽い
- 残り時間を考えると **Cで進めてBを今後の拡張に回す**のが安全

### 論点C: 漂着ゼロのoriginの扱い — 解決済み

300地点中18地点(6%)が90日間漂着ゼロ。除外はせず、`train_pairs.npz`に
`valid`フラグとして保持し、loss側で扱いを分けた(`forward/losses.py`):
cross_entropyは`valid`でマスクして除外(target全0だと勾配が立たず学習に
寄与しないため)、weighted_mseはマスクせずそのまま使う(predがsoftmaxで
sum=1固定なので「薄く広がった分布」への学習信号になる)。

### 論点D: windage係数の較正

- 現状 1〜4% の一様分布を粒子ごとにランダム付与
- 環境省の廃ポリタンク調査では判読分の大半が韓国語表記。一方シミュレーションでは
  日本漂着のうち韓国由来は6.6%
- これは矛盾ではない: ポリタンクは水面上への露出が大きい**高windage品目**であり、
  日本由来ごみは日本語表記なので実測側の分母から外れやすい
- `WINDAGE_MIN=0.03, WINDAGE_MAX=0.05`(ポリタンク相当)で再実行して韓国由来比率が
  どこまで上がるか見ると、実測との対応を定量的に示せる。追加シミュレーション1回で済む

---

## 7. 実測データ(検証用)

学習データとしては粒度が粗すぎるが、**検証(validation)には有用**。

- **環境省 海岸漂着物モニタリング調査** (2010年度〜、全国10地点)
  - 廃ポリタンクの言語表記から製造国を推定した個数が公開されている
  - 例: 平成29年度は18都府県で14,335個確認、判読できた11,191個のうち
    韓国語9,782個 / 中国語416個
  - PDF形式なので、対馬・五島など2〜3地点の内訳を手作業で拾う程度が現実的
- **JEAN / ICC 市民調査** (1990年〜、全国約250箇所) — 品目・数量中心、起源国までは不明

使い道: ①定性チェック(シミュレーションが現実離れしていないか) ②発表での説得力
(「シミュレータ製データだけで完結していない」ことの反証) ③将来のbias-correctionの伏線

---

## 8. 環境

- MacBook Air M5 / 16GB RAM / 空き約300GB。**dedicated GPUなし**
- PyTorchのGPUバックエンドは **MPS** (Apple Silicon)
- Python 3.11.9 (pyenv)
- 主要ライブラリ: `parcels` 3.1.4, `xarray`, `zarr`, `netCDF4`, `scipy`, `pandas`,
  `matplotlib`, `copernicusmarine`, `cdsapi`

### 注意
- **Attention U-NetはPyTorchに標準搭載されていない**。自前実装(Attention Gate込みで
  200行程度)か `segmentation-models-pytorch` 等の外部ライブラリを使う
- **沈降はNNのハイパーパラメータでは扱えない**。シミュレータ側の物理なので、
  やるならデータ生成からやり直しになる

---

## 9. 残り時間とリスク

- **締切: 2026-08-25**(延長後)。1日6時間程度
- 進捗: 順モデル・逆モデル・データ増量(500地点)・Streamlit実装まで完了
  (§2参照)。残りは実測データとの整合性チェックとレポート執筆
- **シミュレーション再実行の時間コストは当初の想定より大幅に小さかった**:
  「300地点×90日は一晩コース」という見積もりは過剰で、実際には
  500地点×200粒子(10万粒子)でも約40秒で完走した。この見積もりのズレのおかげで、
  データ増量(300→500地点、50→200粒子)を締切間際でも気軽に試せた
- ハイパーパラメータ探索は3〜5回の的を絞った試行に留める方針が機能した
  → `sweep_*.py`で3〜5候補の一括比較スクリプトを書く形(`forward/sweep_lr.py`,
  `sweep_wd.py`, `sweep_dropout.py`, `backward/sweep_lr.py`, `sweep_wd.py`参照)
- git運用(main/tani 2ブランチ)で実験ログとコードが一部消失する事故が発生し、
  復旧に時間を使った(§5参照)。以降はmainブランチに一本化

---

## 10. 作業スタイル(引き継ぎ用メモ)

- 率直でリスクを明示した評価を好む。トレードオフを比較して示すこと
- 非現実的な提案には遠慮なく指摘してよい
- まず小規模で end-to-end を通してからスケールする方針
- ハードウェア・ワークフローの制約を常に judgment に含める
- モデル部分は**本人が自分で書きたい**意向。設計の壁打ちと地雷の指摘が主な役割